import styled from "styled-components";

export const SectionTitle = styled.h3`
  font-size: 50px;
  font-weight: 700;
  line-height: 48px;
  letter-spacing: -1px;
  margin-top: 0;
  margin-bottom: 10px;
  padding-bottom: 30px;

  span {
    color: var(--primary-color);
  }
`;
